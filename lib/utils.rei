/** The Utils module define helper functions commonly used throughout the
    project. */

/** Returns a greeting message.

    {4 Examples}

    {[
    print_endline(hello("Jane"));
    ]} */
let greet: string => string;
let hello: unit => string;
let helloInput: string => string;
